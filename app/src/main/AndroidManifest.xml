<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.Store">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.dell.Store.LoginActivity"
            android:label="Store">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.dell.Store.RegisterActivity"
            android:label="Store" />
        <activity
            android:name="com.example.dell.Store.HomeActivity"
            android:label="Home" />
        <activity
            android:name="com.example.dell.Store.AddProductActivity"
            android:label="Add Product" />
        <activity android:name="com.example.dell.Store.EditProductActivity"
            android:label="Edit Product"></activity>
    </application>

</manifest>